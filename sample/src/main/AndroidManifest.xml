<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.kickflip.sample" >

    <uses-permission android:name="android.permission.INTERNET"/>                   <!-- For broadcasting -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>     <!-- For video output -->
    <uses-permission android:name="android.permission.CAMERA"/>                     <!-- For recording from camera -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>               <!-- For recording from microphone -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="io.kickflip.sample.MainActivity"
            android:screenOrientation="landscape"
            android:label="@string/app_name" > <!-- Force Activity that displays Camera preview to Landscape for easier aspect management. TODO: Remove this constraint-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
